"""경기도 토지거래허가 내역 수집 — 서울(seoul/)의 형제 패키지.

⚠️ 서울과 데이터원이 근본적으로 다르다. 서울은 land.seoul.go.kr 이 자치구 단위 JSON API
(getContractList.do)로 허가 처리내역을 깔끔하게 내려준다. 경기도는 그런 도(道) 단위 API 가
없다 — 경기부동산포털(gris.gg.go.kr)은 '허가구역 경계'만 보여주고 처리내역은 없으며, 처리내역은
국토부 K-Geo 플랫폼 자료를 각 시·군·구가 제각각 형식으로 재공개한다.

그래서 이 패키지는 **시(市)별 구현**을 모은다(확장 가능). 현재는 과천시만:
  - 과천시청(gccity.go.kr) 게시판 '토지거래허가내역'(ptIdx=259)에 매월 1건씩
    "토지거래계약 허가사항(YYYY.M.)" HWP 첨부로 올라온다.
  - HWP 의 PrvText(미리보기 텍스트) 스트림이 `<연번><지번><지목><허가일><이용목적>…` 형태의
    깔끔한 표라 olefile 로 읽어 파싱한다(바이너리 표 파싱·Synap 뷰어 불필요).

산출물(PermitDTO)은 서울과 동일한 값 객체라 diff/digest/DB/매칭을 그대로 공유한다.
성남(구청 공시)·용인(정보공개)은 형식이 또 달라 미구현 — 추후 같은 패턴으로 추가한다.
"""
