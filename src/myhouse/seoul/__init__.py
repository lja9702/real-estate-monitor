"""서울시 부동산정보광장(land.seoul.go.kr) 토지거래허가 내역 수집.

네이버(매물·실거래)와 완전히 별개의 데이터원이다. 서울시 지식(엔드포인트·응답 구조·
지번 정규화)을 이 패키지에 가둔다 — 사이트가 바뀌면 여기만 고친다.

핵심: 정부 사이트라 봇 차단이 없어 **httpx 직접 호출**로 충분하다(네이버처럼 Playwright
토큰 우회가 불필요). 응답에는 가격·면적이 없으므로 "거래활성 선행신호"로만 쓴다.
"""
